{
  "name": "color-generator",
  "version": "1.0.0",
  "description": "A random color generator with variants and color sets",
  "main": "dist/src/index.js",
  "source": "index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "test": "tsc && jest --no-cache dist/test",
    "start": "tsc && node",
    "dev": "tsc && parcel --open chrome"
  },
  "author": "Rachel Reilly",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "jest": "^27.4.7",
    "nodemon": "^2.0.15",
    "parcel": "^2.2.1",
    "ts-jest": "^27.1.3"
  },
  "jest": {
    "verbose": true,
    "testPathIgnorePatterns": [
      "tests/jest.config.js"
    ]
  }
}
